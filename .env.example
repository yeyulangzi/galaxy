# 数据库文件路径，留空则使用默认 ~/galaxy/data/galaxy.db
GALAXY_DB_PATH=

# Deep Dive Agent 人格 prompt 文件路径（.md 文件，运行时读取并按 mtime 自动失效缓存）
# 留空则使用 message/route.ts 里的默认路径（指向 Qoder 工作区）
# 在其他机器/容器里运行时，请覆盖为该机器上能访问到的绝对路径；读取失败时会自动降级到内置精简 persona
GALAXY_AGENT_PROMPT_THINKER=
GALAXY_AGENT_PROMPT_PARTNER=
